spring:
  application:
    name: kexi-sys-server
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.2.220:8848
      username: nacos
      password: nacos
      config:
        file-extension: yaml
mybatis-plus:
  configuration:
    # 是否将sql打印到控制面板(该配置会将sql语句和查询的结果都打印到控制台)
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl